#import "../config/thesis-config.typ": glpl

#pagebreak(to:"odd")

= Analisi dei requisiti<cap:analisi-requisiti>

#text(style: "italic", [
    In questo capitolo descrivo lo studio preliminare degli approcci possibili, i casi d'uso e la lista dei requisiti divisi per tipologia e necessità.
])
#v(1em)

== Studio preliminare degli approcci <cap:studio-preliminare>

// qui: il problema del determinismo, RAG e Text-to-SQL valutati e scartati
// per i dati strutturati, la scelta del semantic layer, gli esperimenti
// sull'interfacciamento con i dati (jsonb, viste materializzate, Parquet)

== Casi d'uso

== Classificazione dei requisiti

== Tracciamento dei requisiti

// il template offre in chapters/data/requirements_list.typ la gestione
// automatica delle tabelle di tracciamento (codici F/Q/C - M/D/O)
